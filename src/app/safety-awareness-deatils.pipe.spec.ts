import { SafetyAwarenessDeatilsPipe } from './safety-awareness-deatils.pipe';

describe('SafetyAwarenessDeatilsPipe', () => {
  it('create an instance', () => {
    const pipe = new SafetyAwarenessDeatilsPipe();
    expect(pipe).toBeTruthy();
  });
});
